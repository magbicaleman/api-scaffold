'use strict';

/**
 * log in a user
 * @memberOf HTTP.routes.user
 * @instance
 * @name login
 * @param req
 * @param res
 */
module.exports = function(app) {
  //app.shared.responseHandler.success(res);
};
